- name: Update APT cache
  ansible.builtin.apt:
    update_cache: yes
  when: ansible_os_family == "Debian"

- name: Install basic packages
  ansible.builtin.apt:
    name:
      - curl
    state: present
  when: ansible_os_family == "Debian"

